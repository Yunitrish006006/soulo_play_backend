{
  "name": "soulo-play-backend",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply soulo_play_db --local",
    "db:migrate:remote": "wrangler d1 migrations apply soulo_play_db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.62.0"
  }
}
